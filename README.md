# Core Maths Bridge

An offline-first, low-bandwidth instructional prototype designed from the supplied Instructional Analysis Report â€” Scenario B. It deliberately supports, rather than replaces, print-based materials, peer work, and teacher facilitation.

The header includes a decorative **fictional** Nyameso Community SHS crest created for this pseudonymous academic prototype. Replace it only if the school supplies an authorised official crest.

## Local use

Open `index.html` with any simple local web server. For the offline install prompt and service worker, serve it over HTTPS (Vercel does this automatically).

## Vercel deployment

1. Put these files in a GitHub repository (or drag this folder into the Vercel dashboard).
2. In Vercel, select **Add New â†’ Project**, import the repository, and leave the framework setting as **Other**.
3. Click **Deploy**. No build command is required.
4. Open the generated `https://â€¦vercel.app` address once while online; learners may then use the browserâ€™s install/add-to-home-screen option for cached offline use.

No names, assessment results, or answers are sent to a server. Progress is stored only in the browser on the device being used.
